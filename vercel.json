{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "//": "함수 실행 리전을 서울(icn1)로 고정 — DB(Supabase)가 ap-northeast-2(서울)인데 함수는 기본값 iad1(미 버지니아)에서 돌고 있었다. API 라우트 하나가 DB를 4~5번 순차 왕복하므로 왕복당 ~200ms × 5 ≈ 1초가 순수 네트워크 지연으로 나갔다. 사용자·DB가 전부 한국이라 다른 리전을 쓸 이유가 없다.",
  "regions": ["icn1"],
  "crons": [
    {
      "path": "/api/cron/charge-subscriptions",
      "schedule": "0 1 * * *"
    },
    {
      "path": "/api/cron/monthly-report",
      "schedule": "0 0 * * *"
    },
    {
      "path": "/api/cron/weekly-report",
      "schedule": "0 0 * * *"
    }
  ]
}
